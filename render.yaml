services:
  - type: web
    name: booking-templates
    env: static
    buildCommand: echo "No build required"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /booking-templates.html